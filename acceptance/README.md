# Acceptance test

A reproducible, fresh-context proof that the toolkit's audit dimensions **catch
their known failure classes** on a seeded fixture — the discipline behind the
project's standing rule: *every coverage gap is encoded into a
dimension/scan-family/baseline (proven by a fresh fixture run) — or, for the
determinizable honesty-critical properties, into a `harness/*.mjs` engine guarded
by a standing deterministic test here — never left to a maintainer being clever
in one session.*

| File | What it is |
|---|---|
| `generate-fixture.mjs` | Builds "Helios Service Agent" (`~/srt-helios`) — the **RECALL** fixture: a synthetic Agentforce managed 2GP seeded with one concrete instance of every probe in the `agentforce-package` / `package-metadata` / `apex-exposed-surface` finder dimensions, negative controls, and a deleted-but-recoverable git-history secret. Synthetic secrets are assembled from parts at runtime so this generator stays secret-scan-clean. Scores near-0 / BLOCKED by construction (a catastrophe needs no fine judgment). |
| `expected-findings.md` | Helios's **sealed ground-truth plant list** — the recall grading key. Lives here, never in the fixture, so finders cannot read it. |
| `generate-solano-fixture.mjs` | Builds "Solano Pipeline Guardian" (`~/srt-solano`) — the **MIDDLE-BAND judgment** fixture (docs/roadmap-middle-band-judgment-fixture.md): a **mostly-compliant** Agentforce managed 2GP + companion endpoint (with sharing, CRUD/FLS in user mode, no injection, no live secrets, `installable`) seeded with **6 genuinely contestable issues** across distinct judgment axes (severity-boundary, tempting-but-safe FP, fix-vs-document, partial evidence, deployed-artifact, prompt-hardening-middle). Engineered to land the SCI in the **65–75% band**. Tests precision-and-calibration on the subtle case, not recall. Secret-scan-clean by construction. |
| `solano-adjudication-key.md` | Solano's **sealed adjudication key** — per-issue intended call + why + the defensible-consistency bar (the calibration/precision grading key). Off-fixture (in the repo, never in `~/srt-solano`) so finders cannot read it. For a cold run it is **re-isolated off-repo** (copied to `~/solano-adjudication-key.md` and removed from `main` for the run window, then restored) — stronger isolation than the Helios `expected-findings.md` precedent, since this run's value is honest judgment. See its *Cold-run isolation* section. |
| `build-run-args.mjs` | Mechanically performs the `audit-codebase` run-args step: extracts each applicable dimension's §4 finder prompt + §5/§6 verifier guidance from its dimension file and injects a project-local engine copy. Supports a focused single-dimension re-run. |
| `test-*.mjs` (50 files) | **Standing deterministic tests** — 50 dependency-free, self-asserting test files (**471 checks**) that guard the `harness/` engines + the `hooks/` enforcement hooks: the **consent-coupling guard** (`test-record-consent` — record→verify round-trip, verify-FALSE-on-missing/negative/empty, **DENY-precedence** (a "no"/"don't"/"do not" never records as a yes), clock-free monotonic seq, and the load-bearing **audit-launch-fails-closed** check: `build-audit-engine` refuses to assemble [exit ≠0, no engine written] without the recorded `audit-tier` + `audit-targetmap` consents, and stamps `consentVerified` with them; plus **substrate parity** — the sequential fallback carries the same `verifyConsent`/`record-consent` fail-closed gate (fails the build if it loses it), the gate is in both "survives either substrate" lists; plus scope checks that the journey hard-bounds silence-is-yes to detected inputs, the "don't wait for a go" language is gone, and the journey + audit-codebase use AskUserQuestion + record-consent per gate; plus the **0.8.17 controlled `--decision affirm|deny` token** — the operator's SELECTED AskUserQuestion option is authoritative, the free-text label is recorded but NOT regex-scanned (kills the re-record churn when the label has no "yes" word), deny-precedence holds, and an invalid `--decision` exits 2), the **calibration-FP-pattern presence guard** (`test-calibration-fp-patterns` — asserts the five blind-multi-judge calibration rules [reachability-is-a-precondition / availability≠security / a-missing-grant-is-fail-closed (blind 30-judge) + webhook-HMAC-compute-DoS-is-cheap (blind 15-judge) + packaged-surface-subscriber-can-grant-or-wire (0.8.16)] are present in the §6 Known-false-positive table of each named dimension + the cross-cutting lines in `audit-methodology.md`, so the LLM verifier-guidance prose can't silently regress out; it does NOT test the LLM judgment; 0.8.20 adds the §6 "a contestable-band flip on unchanged code counts as DRY" stop-rule clause presence guard), the **middle-band band-check** (`test-solano-band` — hand-authors the Solano representative scope-manifest + audit-ledger + evidence-index, runs the REAL `compute-sci` against the REAL baseline, and asserts the HONEST post-Phase-A state: the JUDGMENT is clean (0 open critical/high, the block is owner-materials-only with `blocker_findings` EMPTY) while the SCI stays LOW / `BLOCKED` (7%) until Phase B pre-populates owner artifacts — NOT the old 71%; a PRIMARY fixed-126-id layer + a CORROBORATE live-baseline layer that fails loud on a renamed/removed id, so the design "can't silently drift"), the **throwaway-DAST-target detector** (`test-stack-detect` — the 0.7.0 foundation: `runnable`/`needs-recipe`/`needs-secrets`/`n/a` classification + the env classifier that decides which secrets the toolkit can synthesize vs which the owner must supply, driven hermetically on synthetic Node/package-only repos), the **throwaway stand-up + teardown** (`test-standup-stack`/`test-teardown-stack` — the consented stand-up plan + fail-closed-without-consent + the manifest carrying env NAMES not values, and the asymmetric name-scoped teardown that REFUSES a non-`sf-srt-stack-` docker resource; hermetic, no docker), the **scan-tool detector** (`test-tool-detect` — present vs installable-on-consent vs owner/owner-portal, executable-bit, determinism; the 0.6.0 preflight foundation), the **consented scanner installer** (`test-install-scanners` — the deterministic per-method plan + determinism + safe-tmp-root guard incl. degenerate-run-id + shared-grouping-dir rejection + the no-pin→skip rule, and a HERMETIC executor pass with zero network: the consent gate fails closed, `--dry-run` writes a planned manifest, a git clone from a local repo installs, a `file://` download is accepted on a matching sha256 / rejected-without-execution on a mismatch, and extract-to-scratch puts ONLY the verified binary on PATH), the **asymmetric scanner cleanup** (`test-cleanup-scanners` — the tmp tool dir is removed while evidence + sibling paths survive, an unsafe-tmpRoot or SYMLINK tmpRoot is REFUSED removing nothing, idempotent re-run, and resolution via pointer / `--manifest` / `--tmp-root`), SCI fail-closed + determinism + the **reviewer-reproducible credit rule** (`test-sci` P1 cases — an audit-only clear is statically-cleared, never credited, never clears the blocker floor: the 9%→17% self-grading regression), the **evidence-index producer** (`test-build-evidence-index` — provenance adjudication scanner-vs-audit, fail-safe on a missing scanner file, end-to-end into compute-sci), the **mechanical ledger merge** (`test-merge-ledger` — incremental merge, regression flip, redaction, dedup, wrapper shapes; hermetic git repo; + **Track-1b cross-dimension collapse**: same-file + **overlapping-line-span** + different-dimension OPEN dupes merge to ONE entry at the max verified severity with both lenses retained, a different location stays separate, idempotent, + the **over-merge guard** — same file + same method name in both titles but no/non-overlapping spans must stay TWO entries, locking out the title-symbol path the off-disk grade caught conflating two distinct vulns; plus the **0.8.18 unwrap lock** — a RAW Workflow task-output envelope (`{summary, result:{ledger_updates}, workflowProgress}`) merges IDENTICALLY to a pre-extracted `{ledger_updates}` (locks the `merge-ledger.mjs:59` `.result` unwrap so the skill's "point `--result` DIRECTLY at the raw task-output file" promise can't regress), and a `--result` with neither shape → exit 2 with a clear error naming both; plus the **0.8.26 corruption guard** — a PRESENT-but-non-array prior ledger `findings` (a hand-edited / out-of-band write) → a LOUD `[merge-ledger] WARNING` to stderr + exit 2 with the on-disk ledger left untouched, never a silent drop / false-clean), the **cross-run recurrence classifier** (`test-recurrence-confidence` — the 0.8.7 engine that classifies findings across N independent run-ledgers of the same codebase: the all-runs-stable→`confidence=high` reliably-recurring-blocker case, the confirmed→refuted→absent flip→`some_runs`/`review`, the severity-drift (medium/high/medium)→`all_runs`/`review`-not-`high`, the single-run→`investigate`, line-span overlap MATCH vs non-overlap STAY-SEPARATE + absolute-vs-relative path-suffix reconciliation, the **0.8.8 bare-basename over-merge guard** (a single-segment file like `package.json` cited in three different directories stays THREE single_run loci, never one all_runs/high — false confidence is the forbidden direction — while an identical root-level `Dockerfile` cite still merges by exact equality), the **0.8.8 two-phase anti-bridge guard** (a broad refuted finding overlapping two disjoint confirmed defects attaches to one without FUSING them), the **0.8.10 commit-consistency guard** (same `audited_commit` across runs→`consistent` + no mixed-warning; differing→`mixed` + caveat warns code-change-vs-instability; a run missing its commit→`unknown`), the **0.8.10 by_file rollup** (2 loci in fileA + 1 in fileB → 2 file rows, fileA `locus_count=2` with the right confidence tally + `has_reliable_blocker`), the **0.8.10 `--repo-root` relativization** (a path under the root is stripped to display-relative, a path not under it is left intact, matching unaffected), byte-determinism, fail-closed [non-array `findings`→empty; missing/non-JSON ledger→exit 2], the N=1 no-recurrence-signal caveat, hand-checkable pairwise Jaccard (2/3→0.67), and the honesty-caveat presence + no-completeness-assertion guard — INLINE synthetic fixtures, so it runs in CI without the off-repo real ledgers), the **baseline-refutation check** (`test-baseline-refutation-check` — the 0.8.16 v67 auto-enforcement gate: flags `refuted` findings citing platform auto-enforcement [user-mode/with-sharing at API 67.0+] the package `sourceApiVersion` <67.0 doesn't buy — 64→invalid / 68→valid / null→unknown, version precedence `--api-version`>`--sfdx-project`>`--scope-manifest`, `--strict` exits 3, byte-determinism, fail-closed; REPORT-ONLY, gates nothing), the **union-convergence check** (`test-union-convergence` — the 0.8.16 Thread-2 engine: does the union of confirmed loci across N runs STOP growing? a converging set→`converged`+`plateau_run` set, a non-converging set→`converged:false`/`plateau_run:null`, overlapping-line-span locus identity shared with the recurrence engine, OPEN-only, byte-determinism + the ≥2-run guard; REPORT-ONLY, gates nothing), the **audit-engine assembler** (`test-build-audit-engine` — §4/§5 extraction + inject passes injection-check, loud failure on a bad dimension; plus the **0.8.17 engine-enforced always-on dimensions** — `sessionid-egress`/`secrets-credentials`/`error-handling-disclosure` are auto-injected regardless of the driver's scope-input, an always-on key in `na` is forced applicable with a WARN, and nothing duplicates when the driver already lists them), the **artifact-engine assembler** (`test-build-artifact-engine` — the **0.8.21 P2-parity twin** for the ARTIFACT phase: valid `{artifacts,facts,gate}` DATA → `artifact-engine.mjs` carrying repoRoot + the pre-read templates; a missing template / empty focus / absent injection marker each abort loud; **GATE ENFORCEMENT** — a `gate.suppress` artifact is DROPPED + warned, so a withheld doc physically can't be drafted; the template's run-args guard fires against the real source; every-artifact-withheld → exit 2; byte-determinism — so neither phase hand-authors a Workflow with inline prompt strings), the **gate-spec engine** (`test-gate-spec` + `test-tier-no-reask` — the **0.8.22 presentation-consistency Slice 1**: the FROZEN gate catalog + pure `gateOptions(gateId,facts)` selector that PINS each `AskUserQuestion` gate's option set so the driver renders `label`/`description` VERBATIM + pipes the chosen `decision` to `record-consent` — determinism [same `(gate,facts)` twice → byte-identical, incl. via the CLI], golden option snapshots [run-mode's 2-option election / audit-tier's first-pass `{Standard,Exhaustive,Quick,Cancel}` with **Exhaustive OFFERED but never pre-selected** / the WI-02 confirm variant `{Authorize the <locked> launch, Change tier, Cancel}` / scanner-install's VERBATIM sha256 / tmp-removed / evidence-kept / "this yes also covers RUNNING them, which fetches rules" disclosure with only the count + `name(method)` list filled], **FAIL CLOSED** [an unknown gate / an option missing `label`·`description`·`decision` / a decision that is not `affirm|deny` / scanner-install with zero installable scanners each THROW], the **FORCE-INJECTED safe-default decline** present on EVERY consent gate [proven even when the selector's own set omits it — the `ALWAYS_ON` pattern] but NOT on the non-consent run-mode election, every emitted `decision` round-tripping through the real `recordConsent` [`affirm`→affirmative, `deny`→not], and the **WI-02 collect-once flow** [a journey→audit-codebase CLI fixture asserts the tier menu is collected ONCE and the second surface is a confirm, not a re-election; pass-1 never pre-selects Exhaustive; only "Change tier" re-opens the menu] + the journey / audit-codebase render-verbatim wiring), the **operator-output render-verbatim substrate** (`test-render-stability` + `test-readiness-verdict` — the **0.8.23 presentation-consistency Slice 2**, the OUTPUT-class twin of gate-spec: `render-stability.mjs` emits the VERBATIM Finding-Stability block from `recurrence-confidence.json` [determinism; PRESENT (n≥2) = the `bucket_counts` table + the `reliably_recurring_blockers` set + the contestable band named consistently + a mixed-commit note IFF `commit_consistency != consistent`; ABSENT/single-run = one honest one-liner; the "informational only — changes NOTHING about the SCI gate" caveat on BOTH branches, and it NEVER claims complete/passed], and `render-readiness-verdict.mjs` + `templates/operator/readiness-verdict.md.tmpl` PIN the readiness verdict [fill on a frozen fixture → byte-identical twice; the fixed RENDER-sentinel section order; the **`SCI_BLOCK` slot equals `compute-sci` stdout BYTE-FOR-BYTE**; the standing caveat equals the single committed `STANDING_CAVEAT` constant, force-injected so a caller value is OVERRIDDEN (un-paraphrasable); **FAIL CLOSED** — a missing slot leaves a `{{SLOT}}` → `fillVerdict` THROWS, a template without the caveat slot THROWS, no `{{…}}` survives a real fill; the `lintRenderVerbatim` flags a hand-built Markdown table for a surface with a registered renderer/template, and the real compile-submission skill passes by routing through the template]), the **entry-experience render-verbatim set** (`test-finding-clusters-headline` + `test-render-target-map` + `test-render-preflight` + `test-render-scan-status` + `test-render-router-status` + `test-render-recap` — the **0.8.24 presentation-consistency Slice 3**, pinning the six most-seen ENTRY surfaces: the **finding-cluster triage headline** [`renderClusterHeadline` + `--headline` — raw per-severity counts FIRST then the clustered distinct-file view; the SAME block at the audit exec summary AND the journey blocker gate, so the FAILURE VERDICT reads byte-identically; NONE → an honest "nothing new" line, a missing ledger → "unavailable", never a false clean], the **target-map approval** [fixed `{dimension|applicable|targets|why|confidence|unresolved}` table, applicable rows FIRST, unresolved flagged, pipe/newline-escaped cells], the **3-tier preflight** [`DEEP_AUDIT_STATES` is EXACTLY the four states + `deepAuditState` is TOTAL over package-readiness's outputs (installable / needs-build+registered / needs-build+!registered / no-package); the three tiers render in fixed order; empty/null facts → all-"not detected", never fabricated], the **scan-status summary** [the frozen 8-row `SCAN_FAMILIES` in canonical Family 1–8 order with locked columns; DONE needs a reviewer-reproducible report on disk, a pending-owner plan → PENDING, a structural-N/A → N/A; the manifest drives Applies, no manifest → "?"], the **router "where are we?"** [exactly 3 lines; the phase ladder maps each furthest-reached phase to the right next skill; drift → re-scope, a stale ledger → re-audit; null → fresh start], and the **end-of-run recap** [LED BY `renderClusterHeadline` — a byte-identical-lead assertion proves the recap embeds the headline verbatim; the counts line; PROCEED/HALT from the open critical/high; the fixed not-covered caveat; merge-ledger imports + emits it to stdout]; each test asserts determinism + a golden snapshot of the fixed structure + fail-safe on missing/non-JSON input + the skill-wiring [grants + references the harness + states "print verbatim"]), the **scope-submission REPORT render-verbatim set** (`test-render-detected-elements` + `test-render-mcp-scope` + `test-render-sf-autoresolve` + the extended `test-applicable-requirements` — the **0.8.25 presentation-consistency Slice 4**, WI-06 reports half, pinning the five scope-submission REPORT surfaces: the **detected-architecture-elements summary** [INV-15 — the fixed `{Element|Detected how (evidence)}` table in `CANONICAL_ELEMENT_ORDER`, unknown types appended/never dropped, the listingType line, a no-evidence cell honest, empty → "scope not detected yet"], the **applicable-requirements presentation** [INV-16 — `applicable-requirements.mjs --render`: count = exact list length, ids by track, the conflicting-requirements section surfacing the `endpoint-ssl-labs-a-grade` `conflicting` entry WITH its folded-block-scalar `conflicts` text per CONVENTIONS §4, the mobile-no-coverage gap line when a mobile element is in scope, empty → "scope not computed yet"; plus the `parseBaselineApplies` verification/conflicts parse], the **MCP direction + auth profile** [INV-43 — B/A/both captions + the auth-profile fields rendered straight from `mcp.authExpectations`, NOT re-derived; missing authExpectations → honest, never fabricated], the **MCP live-probe result** [INV-44 — `probed:true` → "live-probed", `probed:false` → "recorded from code, NOT live-probed" (an un-probed fact is NEVER presented as a probe), an absent field → "(not recorded)"], and the **SF-CLI auto-resolution** [INV-45 — the rows table + a Security-flags section DERIVING every class (http:// non-TLS · wildcard · no-Named-Credential · ViewAll/ModifyAll over-grant), merged/deduped, never dropped + a Conflicts section (CLI is evidence, not an override); gated on the manifest `sfAutoResolved` → "auto-resolution skipped" when it didn't run; and a **secret guard** — a secret-named key / token-shaped value is REDACTED, never echoed (CONVENTIONS §6); **0.8.26** extends the secret guard to EVERY rendered cell (derived-flag host, row/endpoint/flag/conflict source, recorded-flag detail, conflict autoResolved), not just row values, and softens the docstring from an overclaimed "never" to its honest producer-is-the-boundary scope (SA6)]; plus the **dict-vs-array honesty guard** folded into `test-finding-clusters-headline` + `test-render-recap` [a PRESENT-but-non-array `findings` renders UNAVAILABLE, never NONE/PROCEED — the CLAUDE-rule-8 corollary; **0.8.26** extends it to the `render-recap.mjs --target` path — `factsFromLedger` preserves the dict shape instead of coercing it to `[]`, so the standalone re-render is UNAVAILABLE not a false PROCEED (RC7)], the **dimension-extraction coverage** (`test-dimension-extraction` — drives build-audit-engine over EVERY `methodology/dimensions/*.md` so every current + future dimension's §4/§5 markers + non-empty finder/verifier prompts are guarded, not just two hand-picked keys), the artifact gate on every entry path (incl. the audit-only auto-proceed + the authN/authZ withhold), the **PreToolUse hook** (no-op unless armed + writing the gated artifact → denies; fail-closed), the **sf-ops safety gate** (`test-sf-ops-gate-hook` — the 0.8.11 fail-closed consent gate for irreversible sf/host ops: each of the 3 gate classes [`sf-package-promote` / `sf-deep-audit-ops` / `sf-cli-setup`] classified verb-based and DENIED without a recorded consent / ALLOWED with one; the PERMANENT `sf package version promote` is its OWN gate [a deep-audit consent does not authorize it, and vice-versa] with a permanence-emphasizing deny; read-only + benign commands pass [`sf org list`, `sf package version list`, `--help`, `git push`, `ls`, local `npm install`]; variant normalization [colon form, `sfdx force:`, leading `FOO=bar sudo`, whitespace, a CHAINED command DENIED on its promote segment]; scope + fail-closed [no `.security-review/` up-tree → ALLOW, malformed/empty payload → ALLOW so arbitrary Bash is never blocked, a classified op with no consent → DENY] + the real CLI deny-JSON/allow-no-stdout path; plus the **0.8.12 adversarial bypass battery** — ~36 wrapped/normalized forms of the same irreversible op that must all DENY [CLI paths `/usr/local/bin/sf`/`./sf`, quoted/escaped CLI `"sf"`/`\sf`, background `&`/`|&`, grouping `(…)`/`{…;}`/`((…))`, wrappers `command`/`exec`/`time`/`nice`/`nohup`/`watch`/`sudo -u nobody`/`env -i`, `sh -c`/`eval` (incl. a separator INSIDE the quoted inner command + a chained `… && bash -c "…"`), interspersed verb-scan flags `--json`/`-o foo`/`--org=foo`/`--`/leading-flag-before-CLI, the newly-gated `package delete`/`package version delete`/`sandbox create`/`sandbox delete`/`npm uninstall -g`], a benign no-false-denies set, and the EXOTIC residual [`$(…)`/backticks/`$CMD`/`source <(…)`/base64-eval] that stays ALLOW by design), the **deployed-audit readiness** check (placeholder/`needs-build` vs `installable`), element-precise applicability, baseline-count consistency (incl. the **0.8.20 `--currency` emission** — `test-baseline-counts` asserts `newest_verified`/`newest_verified_count`/`oldest_verified` with `null`/malformed date tokens EXCLUDED from the ISO-lexicographic ranking, so a malformed token can never out-rank a real date), **baseline-entry integrity** (`test-baseline-integrity` — every entry's `applies_to` tokens / `severity_if_missing` / the `verification ⟺ last_verified` per-entry implication, plus the 2026-06-20 PMD-rule prediction presence), **prose hygiene** (`test-prose-hygiene` — the CONVENTIONS §9 "simply" ban + the §3 partner-internal-symbol ban, encoded after an adversarial audit caught a voice/genericization slip so it can't recur), cross-dimension de-dup, the audit-engine **injection pre-launch check** (decoy-anchored), the **CI-workflow hygiene lock** (`test-ci-hygiene` — asserts `.github/workflows/test.yml` declares a top-level least-privilege `permissions: contents: read` block and NO write scope, so the public-repo workflow's `GITHUB_TOKEN` can never silently widen; flagged by an external security audit, 0.8.19), the **failure-log clamp** (`test-clamp-log` — the 0.8.20 head+tail truncation `clampLog` keeps the ROOT CAUSE at the top of a deep stack trace, not just the tail; a short string is unchanged, a long one is bounded with an elision marker; used by `run-dast` + `install-scanners` in place of tail-only `.slice(-1500/-2000)`), and ledger staleness — across three layers: the pure `staleFindings` unit test, a **hermetic detect-path test** (`-detect`), and an **adversarial test** (`-adversary`). No LLM, no fixture, no scanners needed — `node` runs them. |
| `acceptance-report-<date>.md` | The graded result of a fixture run: per-class recall, precision on the negative controls, and every gap the run surfaced (each encoded into the toolkit and re-proven). |
| `integration-pass-condition-<ver>.md` | The PRE-COMMITTED, write-before-run pass condition for a version's full-journey validation — authored before the run, graded **cold in a fresh restarted session** off disk. A clean pass gates that version's release **tag**. (`-0.6.0` [consented-scanner-install] + `-0.7.0` [throwaway-DAST + the credential contract] **both PASSED** in the 2026-06-19 cold run — one full Atlas journey graded off disk → **`v0.7.0` tagged**; `-0.5.1`/`-0.5.2` passed earlier, checkpoints tagged through v0.5.5.) |

## Run it

```bash
node acceptance/generate-fixture.mjs ~/srt-helios
# scope the fixture (a fresh agent following scope-submission, or the skill itself)
#   → writes ~/srt-helios/.security-review/{scope-manifest,target-map}.json
node acceptance/build-run-args.mjs "$PWD" ~/srt-helios 2026-06-15
#   → Workflow({scriptPath: "~/srt-helios/.security-review/audit-engine.mjs"})
gitleaks git ~/srt-helios --redact     # run-scans Family 6, history pass
```

Then grade the confirmed findings against `expected-findings.md`. A planted class
the run misses means the encoding was not forceful enough — sharpen the dimension
file (not the session) and re-run. A negative control that gets flagged means the
verifier over-fires — tighten the §5/§6 refute rules.

## Standing tests (deterministic — no fixture, no LLM, no scanners)

The `harness/` engines (and the `hooks/` enforcement hook) that encode the
honesty-critical determinizable properties each carry a self-asserting test. Run
the whole suite (exit 0 = pass; the file/check count grows each checkpoint — the
pass is ZERO failures, not a fixed number; see the table at the top of this file
for the current totals):

```bash
for t in acceptance/test-*.mjs; do node "$t" || exit 1; done
```

They assert, respectively: the SCI fails CLOSED on an empty/missing manifest and
is byte-identical on re-run (currency floor included); the **artifact gate** is
audit-only (clean / flagged — there is NO STOP mode) and withholds the AuthN/AuthZ
artifact only for an open critical/high authN/authZ finding, **failing SAFE
(withhold) on a malformed non-array ledger** rather than reading it as clean; the
**G4 PreToolUse hook** is a no-op unless armed + writing the gated artifact, denies
over an open hole, and **fails CLOSED** on an unreadable/malformed ledger; the
**audit-engine injection check** anchors past the header-comment decoy; the
**deployed-audit readiness** check reads `installable` / `needs-build` /
`no-package` and is bound to the configured package (an unrelated/dependency `04t`
alias cannot fake `installable`); applicability drops agentforce-* / mcp-* for a
plain package and keeps them for a real agent/MCP one; the baseline self-description
counts match the prose; cross-dimension findings de-dup to a distinct-file
headline; and ledger staleness flags findings whose code changed since their
`audited_commit` — proven at three layers: the pure `staleFindings` unit test, a
hermetic detect-path test that drives the git-shelling CLI against a throwaway
repo, and an adversarial test over the messy real-world `finding.file` shapes
(comma/range suffixes, two-file cites, absolute paths). A refactor that breaks any
of these fails the test — that is what makes the enforcement real rather than
narrated.

The fixture is intentionally *not* committed (it carries planted vulnerabilities
and a git-history secret); it is regenerated on demand from the deterministic
generator, which keeps the proof reproducible without shipping a vulnerable
package or a live-shaped secret in this repo.
