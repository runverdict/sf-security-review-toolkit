# Acceptance test

A reproducible, fresh-context proof that the toolkit's audit dimensions **catch
their known failure classes** on a seeded fixture — the discipline behind the
project's standing rule: *every coverage gap is encoded into a
dimension/scan-family/baseline (proven by a fresh fixture run) — or, for the
determinizable honesty-critical properties, into a `harness/*.mjs` engine guarded
by a standing deterministic test here — never left to a maintainer being clever
in one session.*

| File | What it is |
|---|---|
| `generate-fixture.mjs` | Builds "Helios Service Agent" (`~/srt-helios`) — the **RECALL** fixture: a synthetic Agentforce managed 2GP seeded with one concrete instance of every probe in the `agentforce-package` / `package-metadata` / `apex-exposed-surface` finder dimensions, negative controls, and a deleted-but-recoverable git-history secret. Synthetic secrets are assembled from parts at runtime so this generator stays secret-scan-clean. Scores near-0 / BLOCKED by construction (a catastrophe needs no fine judgment). |
| `expected-findings.md` | Helios's **sealed ground-truth plant list** — the recall grading key. Lives here, never in the fixture, so finders cannot read it. |
| `generate-solano-fixture.mjs` | Builds "Solano Pipeline Guardian" (`~/srt-solano`) — the **MIDDLE-BAND judgment** fixture (docs/roadmap-middle-band-judgment-fixture.md): a **mostly-compliant** Agentforce managed 2GP + companion endpoint (with sharing, CRUD/FLS in user mode, no injection, no live secrets, `installable`) seeded with **6 genuinely contestable issues** across distinct judgment axes (severity-boundary, tempting-but-safe FP, fix-vs-document, partial evidence, deployed-artifact, prompt-hardening-middle). Engineered to land the SCI in the **65–75% band**. Tests precision-and-calibration on the subtle case, not recall. Secret-scan-clean by construction. |
| `solano-adjudication-key.md` | Solano's **sealed adjudication key** — per-issue intended call + why + the defensible-consistency bar (the calibration/precision grading key). Off-fixture (in the repo, never in `~/srt-solano`) so finders cannot read it. For a cold run it is **re-isolated off-repo** (copied to `~/solano-adjudication-key.md` and removed from `main` for the run window, then restored) — stronger isolation than the Helios `expected-findings.md` precedent, since this run's value is honest judgment. See its *Cold-run isolation* section. |
| `build-run-args.mjs` | Mechanically performs the `audit-codebase` run-args step: extracts each applicable dimension's §4 finder prompt + §5/§6 verifier guidance from its dimension file and injects a project-local engine copy. Supports a focused single-dimension re-run. |
| `test-*.mjs` (56 files) | **Standing deterministic tests** — 56 dependency-free, self-asserting test files (**721 checks**) that guard the `harness/` engines + the `hooks/` enforcement hooks: the **consent-coupling guard** (`test-record-consent` — record→verify round-trip, verify-FALSE-on-missing/negative/empty, **DENY-precedence** (a "no"/"don't"/"do not" never records as a yes), clock-free monotonic seq, and the load-bearing **audit-launch-fails-closed** check: `build-audit-engine` refuses to assemble [exit ≠0, no engine written] without the recorded `audit-tier` + `audit-targetmap` consents, and stamps `consentVerified` with them; plus **substrate parity** — the sequential fallback carries the same `verifyConsent`/`record-consent` fail-closed gate (fails the build if it loses it), the gate is in both "survives either substrate" lists; plus scope checks that the journey hard-bounds silence-is-yes to detected inputs, the "don't wait for a go" language is gone, and the journey + audit-codebase use AskUserQuestion + record-consent per gate; plus the **0.8.17 controlled `--decision affirm|deny` token** — the operator's SELECTED AskUserQuestion option is authoritative, the free-text label is recorded but NOT regex-scanned (kills the re-record churn when the label has no "yes" word), deny-precedence holds, and an invalid `--decision` exits 2), the **calibration-FP-pattern presence guard** (`test-calibration-fp-patterns` — asserts the five blind-multi-judge calibration rules [reachability-is-a-precondition / availability≠security / a-missing-grant-is-fail-closed (blind 30-judge) + webhook-HMAC-compute-DoS-is-cheap (blind 15-judge) + packaged-surface-subscriber-can-grant-or-wire (0.8.16)] are present in the §6 Known-false-positive table of each named dimension + the cross-cutting lines in `audit-methodology.md`, so the LLM verifier-guidance prose can't silently regress out; it does NOT test the LLM judgment; 0.8.20 adds the §6 "a contestable-band flip on unchanged code counts as DRY" stop-rule clause presence guard; **0.8.29 (Slice 2)** adds the engine-absent → KEEP guard — `apex-exposed-surface` §5/§6 conditions defer-to-SFGE on a `code-analyzer-*.json` proving the engine ran, so a verifier can't drop a real FLS finding via a phantom hand-off to a scanner that never ran [the fixrun4 dropped-blocker]), the **middle-band band-check** (`test-solano-band` — hand-authors the Solano representative scope-manifest + audit-ledger + evidence-index, runs the REAL `compute-sci` against the REAL baseline, and asserts the HONEST post-Phase-A state: the JUDGMENT is clean (0 open critical/high, the block is owner-materials-only with `blocker_findings` EMPTY) while the SCI stays LOW / `BLOCKED` (7%) until Phase B pre-populates owner artifacts — NOT the old 71%; a PRIMARY fixed-126-id layer + a CORROBORATE live-baseline layer that fails loud on a renamed/removed id, so the design "can't silently drift"), the **throwaway-DAST-target detector** (`test-stack-detect` — the 0.7.0 foundation: `runnable`/`needs-recipe`/`needs-secrets`/`n/a` classification + the env classifier that decides which secrets the toolkit can synthesize vs which the owner must supply, driven hermetically on synthetic Node/package-only repos), the **throwaway stand-up + teardown** (`test-standup-stack`/`test-teardown-stack` — the consented stand-up plan + fail-closed-without-consent + the manifest carrying env NAMES not values, and the asymmetric name-scoped teardown that REFUSES a non-`sf-srt-stack-` docker resource; hermetic, no docker), the **scan-tool detector** (`test-tool-detect` — present vs installable-on-consent vs owner/owner-portal, executable-bit, determinism; the 0.6.0 preflight foundation; **0.8.41 T7** flips the `code-analyzer` family from owner-gated to the installable `code-analyzer-stack` method — absent `sf` → installable not owner, present `sf` → satisfied/used-as-is, with the ~1 GB / JDK-11+ footprint note), the **consented scanner installer** (`test-install-scanners` — the deterministic per-method plan + determinism + safe-tmp-root guard incl. degenerate-run-id + shared-grouping-dir rejection + the no-pin→skip rule, and a HERMETIC executor pass with zero network: the consent gate fails closed, `--dry-run` writes a planned manifest, a git clone from a local repo installs, a `file://` download is accepted on a matching sha256 / rejected-without-execution on a mismatch, and extract-to-scratch puts ONLY the verified binary on PATH; **0.8.41 `CA*`** adds the **Code Analyzer cold-install stack** — the compound `code-analyzer-stack` plan (pinned `@salesforce/cli` + `code-analyzer` plugin + a JDK), the **hermeticity-contract structural assertion** (every CA-stack env path + `pathPrepend` entry under the tmp root — the standing guard for the spike's load-bearing finding), `JDK_PINS` integrity (4 platforms, hex64 sha256, the `%2B`-encoded Temurin URL), the JDK detect-or-provision decision (present `java`≥11 → reuse / absent → provision the pin), `installCommands`, and a hermetic `--dry-run` env/pathPrepend disclosure; **0.8.42 `CA7`/`CA8`** add the EXECUTOR coverage of the JDK **verify-before-extract fail-closed** — a `file://` checksum MISMATCH → `status:'failed'` BEFORE any extract, the JDK never unpacked + the pinned-CLI npm never run + no PATH entry (the standing proof of the cold-install slice's headline security guard; mutation-proven RED if the guard is removed) — and the `code-analyzer-stack` **name-membership guard** (an unknown tool name → skip, never an install, mirroring the pip/npm/git/binary allow-lists)), the **asymmetric scanner cleanup** (`test-cleanup-scanners` — the tmp tool dir is removed while evidence + sibling paths survive, an unsafe-tmpRoot or SYMLINK tmpRoot is REFUSED removing nothing, idempotent re-run, and resolution via pointer / `--manifest` / `--tmp-root`), SCI fail-closed + determinism + the **reviewer-reproducible credit rule** (`test-sci` P1 cases — an audit-only clear is statically-cleared, never credited, never clears the blocker floor: the 9%→17% self-grading regression), the **evidence-index producer** (`test-build-evidence-index` — provenance adjudication scanner-vs-audit, fail-safe on a missing scanner file, end-to-end into compute-sci), the **mechanical ledger merge** (`test-merge-ledger` — incremental merge, regression flip, redaction, dedup, wrapper shapes; hermetic git repo; + **Track-1b cross-dimension collapse**: same-file + **overlapping-line-span** + different-dimension OPEN dupes merge to ONE entry at the max verified severity with both lenses retained, a different location stays separate, idempotent, + the **over-merge guard** — same file + same method name in both titles but no/non-overlapping spans must stay TWO entries, locking out the title-symbol path the off-disk grade caught conflating two distinct vulns; plus the **0.8.18 unwrap lock** — a RAW Workflow task-output envelope (`{summary, result:{ledger_updates}, workflowProgress}`) merges IDENTICALLY to a pre-extracted `{ledger_updates}` (locks the `merge-ledger.mjs:59` `.result` unwrap so the skill's "point `--result` DIRECTLY at the raw task-output file" promise can't regress), and a `--result` with neither shape → exit 2 with a clear error naming both; plus the **0.8.26 corruption guard** — a PRESENT-but-non-array prior ledger `findings` (a hand-edited / out-of-band write) → a LOUD `[merge-ledger] WARNING` to stderr + exit 2 with the on-disk ledger left untouched, never a silent drop / false-clean), the **deterministic scanner-ingest foundation** (`test-ingest-scanner-findings` — the 0.8.28 Phase-1·Slice-1 guard over `harness/ingest-scanner-findings.mjs`, driving REAL captured Code Analyzer output from `acceptance/fixtures/`: determinism [ingest the real Solano fixture twice → byte-identical findings], the anchor [the `ApexCRUDViolation` on `SolanoAccountInsightController.cls:19` — the Contact-PII FLS the LLM ledger dropped — lands `provenance:'deterministic'`, `engine:'pmd'`, class-severity `high`], severity-FROM-CLASS [mutating `violation.severity` does NOT move a mapped finding; `classSeverity` reads `fail-crud-fls`/`fail-sharing-model`=`major`→`high` straight from the baseline; the canonical `REQ_SEVERITY_TO_FINDING`/`CA_SEVERITY_TO_FINDING` maps], the sharing class [SFGE `DatabaseOperationsMustUseWithSharing`→`high`/`sfge`] and the ViewAll/ModifyAll source-scanner [a `viewAllRecords=true` custom-object over-grant → `metadata`/`viewall-overgrant`/`high`/`admin-surface`; a STANDARD object + a non-over-grant are NOT flagged], unmapped-but-KEPT [an unknown rule is still ingested as deterministic with the CA-severity fallback + a note, never dropped], idempotent + additive merge [re-ingest → no duplicates; a pre-existing llm-inferred finding survives; a corrupted non-array `findings` ledger is refused], fail-safe [missing/non-JSON/empty input → no findings, no crash], schema conformance [a focused `$defs/finding` validator: a deterministic finding validates, a legacy llm-inferred finding validates (provenance defaults), a deterministic finding missing `engine` FAILS the conditional], and the 2-adapter / 2-kind registry [`code-analyzer` file-parser + `metadata-viewall` source-scanner, each `{name,kind,collect,parse,classify}`] + the CLI for both; **0.8.29 (Slice 2)** adds the **Security/AppExchange tag filter** [a non-security CA rule (`ApexDoc`, `Documentation`/`BestPractices`) → 0 findings, the `Performance`-tagged `MissingNullCheckOnSoqlVariable` filtered out of the real fixture (6 violations → 4 findings), an unmapped SECURITY rule STILL ingested — a filter on non-security noise, never a drop of a security finding] + the mapped finding's owned-`class` label; **0.8.31 (Phase 2 · 2a #1)** adds the **`checkov` adapter** [the `CK*` checks over genuine Checkov 3.3.2 dockerfile output in `acceptance/fixtures/checkov-dockerfile-solano.json`: determinism, the `CKV_DOCKER_2` anchor → `provenance:'deterministic'`/`engine:'checkov'`/`class:'iac-misconfig'`/`dimension:'infrastructure-iac'`/class-severity `high`/`Dockerfile:1` with the guideline URL in the reasoning, FAILED-ONLY (the 24 `passed_checks` → 0 findings), severity-FROM-CLASS (an enterprise `severity:LOW` stays high — Checkov OSS severity is `null`, the tool number never moves it), the multi-framework ARRAY shape → distinct findings, multiple+skip, the CONSTANT `classify()`→`iac-misconfig` + NO `securityRelevant` (security-by-construction, like `metadata-viewall`), malformed-check skip, fail-safe, idempotent merge, schema conformance, and the CLI dry-run + merge; `AD1` now asserts the **3-adapter** registry — `checkov` joins `code-analyzer` as a file-parser]; **0.8.32 (Phase 2 · 2a #2)** adds the **`semgrep` adapter** [the `SG*` checks over genuine Semgrep OSS output in `acceptance/fixtures/semgrep-{coldstart-full,helios}.json`: the FIRST **`tool→band`** adapter — determinism, the WARNING anchor (`dynamic-urllib`/SSRF on `mcp/server.py:76`) → `provenance:'deterministic'`/`engine:'semgrep'`/`dimension:'external-sast'`/**`medium`** (the tool band, not a class)/NO `class` key/the metadata reference URL in the reasoning, the ERROR anchor (`detect-child-process`/CWE-78 on `server/index.js:28`) → **`high`**, two-distinct (same `check_id` at lines 76 & 89 → 2 findings), **severity-FROM-TOOL-BAND** (mutating `extra.severity` `WARNING→ERROR` MOVES the band `medium→high` — INTENTIONALLY the INVERSE of `S1`/`CK-severity-from-class`, with an in-test comment forbidding "harmonization"), the CONSTANT `classify()`→`null` + NO `securityRelevant` + no `class` (owns no toolkit class → supersedes nothing, cross-engine dedup deferred to §10 ext #3/Phase-2b), unknown-severity (`INVENTORY` → `info` with a note, never dropped), the `SEMGREP_SEVERITY_TO_FINDING` map shape, two `buildFinding` unit checks (the new tool→band path AND the **mapped-path regression** — a deliberately-low `bandFromTool` cannot pull a `crud-fls` finding off its class severity, so the MAPPED branch is provably UNCHANGED), fail-safe, idempotent merge, schema conformance, and the CLI dry-run + merge; `AD1` now asserts the **4-adapter** registry — `semgrep` joins as the third file-parser]; **0.8.33 (Phase 2 · 2a #3)** adds the **`bandit` adapter** [the `BN*` checks over genuine Bandit output in `acceptance/fixtures/bandit-coldstart-full.json` — the SECOND **`tool→band`** adapter and the PROOF the Semgrep generalization GENERALIZES with **ZERO harness-core change** (reuses `buildFinding`'s `bandFromTool`; one new adapter + the `BANDIT_SEVERITY_TO_FINDING` map, no `buildFinding`/`CLASS_DEFS` edit): determinism, the `B608` `hardcoded_sql_expressions`/CWE-89 anchor on `mcp/server.py:46` → `provenance:'deterministic'`/`engine:'bandit'`/`dimension:'external-sast'`/**`medium`** (`MEDIUM` band, not a class)/NO `class` key/the `more_info` URL in the reasoning, count (4 findings, all `medium`), two-distinct (`B310` at lines 76 & 89 → 2 findings), the inline `HIGH→high`/`LOW→low`/`CRITICAL→info-never-dropped` band synthetics (also exercising the `more_info`-vs-`issue_cwe.link` resources fallback), **severity-FROM-TOOL-BAND** (mutating `issue_severity` `MEDIUM→HIGH` MOVES the band `medium→high` — the same deliberate behaviour as `SG`, the INVERSE of `S1`/`CK-severity-from-class`), the CONSTANT `classify()`→`null` + NO `securityRelevant` + no `class`, the `BANDIT_SEVERITY_TO_FINDING` map shape, fail-safe, idempotent merge, schema conformance, and the CLI dry-run + merge; `AD1` now asserts the **5-adapter** registry — `bandit` joins as the fourth file-parser]; **0.8.34 (Phase 2 · 2a #4)** adds the **`njsscan` adapter** [the `NJ*` checks over genuine njsscan 0.4.3 output in `acceptance/fixtures/njsscan-solano.json` — the THIRD **`tool→band`** adapter and the FIRST with a DIFFERENT input shape (a NESTED object `{nodejs:{…},templates:{…}}` keyed by rule_id, not a flat `results[]`, so its OWN `parse` reading BOTH sections + deriving the CWE URL from a `CWE-###` prefix): determinism, the `node_secret`/CWE-798 ERROR anchor on `server/index.js:23` → `provenance:'deterministic'`/`engine:'njsscan'`/`dimension:'external-sast'`/**`high`** (the tool band, not a class)/NO `class` key/the derived CWE URL in the reasoning, the `helmet_feature_disabled`/CWE-693 WARNING anchor on `server/index.js:14` → **`medium`**, count (exactly 2), the **templates-section** + **multi-file** synthetics (BOTH sections read; one finding per file occurrence), **severity-FROM-TOOL-BAND** (mutating `metadata.severity` `WARNING→ERROR` MOVES the band — same as `SG`/`BN`), the CONSTANT `classify()`→`null` + NO `securityRelevant` + no `class`, the `NJSSCAN_SEVERITY_TO_FINDING` map shape, the no-CWE/INFO/unknown-band synthetics, fail-safe over every degenerate nested shape, idempotent merge, schema conformance, and the CLI dry-run + merge; `AD1` now asserts the **6-adapter** registry — `njsscan` joins as the fifth file-parser]; **0.8.35 (Phase 2 · 2a #5)** adds the **`gitleaks` adapter** [the `GL*` checks over the genuine gitleaks fixture in `acceptance/fixtures/gitleaks-coldstart-full.json` (leak-clean by construction — synthetic redacted secrets, blanked author/email/message) — the DESIGN PIVOT BACK to **`class`-severity** (like checkov, NOT `tool→band`): determinism, the `generic-api-key` anchor on `mcp/server.py:27` → `provenance:'deterministic'`/`engine:'gitleaks'`/`class:'hardcoded-secrets'`/`dimension:'secrets-credentials'`/class-severity **`high`** (from `fail-hardcoded-secrets`=major, NO tool tier to move it), count (exactly 3), the **load-bearing secret-never-leaks invariant** (a synthetic finding carrying a fake secret + PII in EVERY sensitive field — `Match`/`Secret`/`Message`/`Author`/`Email` — leaks NONE of it into ANY finding field, because `parse()` reads ONLY `RuleID`/`File`/`StartLine`/`Description` and `buildFinding`'s `redact()` is just a backstop), the **LLM-supersession** (a gitleaks finding SUPERSEDES a co-located LLM `secrets-credentials` finding — the FIRST adapter to own a class AND a real methodology dimension), severity-FROM-CLASS, the CONSTANT `classify()`→`hardcoded-secrets` + NO `securityRelevant`, fail-safe over non-array/degenerate input, idempotent merge, schema conformance, and the CLI dry-run + merge; `AD1` now asserts the **7-adapter** registry — `gitleaks` joins as the sixth file-parser]; **0.8.36 (Phase 2 · 2a #6)** adds the **`detect-secrets` adapter** [the `DS*` checks over genuine detect-secrets 1.5.0 output in `acceptance/fixtures/detect-secrets-solano.json` (the `results` object keyed by 6 files, 24 occurrences, 3 detector types — `Secret Keyword`/`Hex High Entropy String`/`Base64 High Entropy String`; leak-safe by design — every occurrence carries only a `hashed_secret` SHA, no plaintext) — the secrets SIBLING of gitleaks that **REUSES the `hardcoded-secrets` class** (NO new `CLASS_DEFS` entry, NO `buildFinding` change; the only shared-file touch is the `ADAPTERS` registry line): determinism, the `Secret Keyword` anchor on `.security-review/audit-engine.mjs:181` → `provenance:'deterministic'`/`engine:'detect-secrets'`/`class:'hardcoded-secrets'`/`dimension:'secrets-credentials'`/class-severity **`high`** (from `fail-hardcoded-secrets`, NO tool tier to move it), count + multi-file (exactly 24 findings spanning 6 distinct files + ≥2 detector types, distinct ids — exercising the **nested-by-file `parse`**), the **load-bearing hash/secret-never-leaks invariant** (a synthetic occurrence carrying a fake `hashed_secret` AND a synthetic `--show-secrets` plaintext leaks NEITHER into ANY finding field — `parse()` emits ONLY `type`/file/`line_number`), **reuses-class** (the constant `classify()`→`hardcoded-secrets` is the SAME class entry gitleaks uses — one definition, two adapters), the **LLM-supersession** (a detect-secrets finding supersedes a co-located LLM `secrets-credentials` finding), the **two-deterministic-coexist** (a detect-secrets finding AND a gitleaks finding at the SAME locus both stay `confirmed` — a deterministic finding never supersedes another deterministic finding, so the cross-engine duplicate is VISIBLE; collapsing it is §10 ext #3, Phase-2b), fail-safe over the nested/degenerate shapes, idempotent merge, schema conformance, and the CLI dry-run + merge; `AD1` now asserts the **8-adapter** registry — `detect-secrets` joins as the seventh file-parser]; **0.8.37 (Phase 2 · 2a #7)** adds the **`osv` adapter** [the `OSV*` checks over genuine OSV-Scanner output in `acceptance/fixtures/osv-coldstart-full.json` (1 source `mcp/requirements.txt`, 3 PyPI packages, 11 vulns; the one host-absolute lockfile path genericized) — the dependency-CVE / SCA adapter and with it **Extension A: the CVSS→enum advisory-severity fork** (the FIRST adapter whose band is neither a class nor a tool tier but a per-advisory CVSS): determinism, count + band-mix (exactly 11 findings, distinct ids, 1 critical · 3 high · 6 medium · 1 low), the `GHSA-82w8-qh3p-5jfq` anchor on `mcp/requirements.txt` → `provenance:'deterministic'`/`engine:'osv'`/`dimension:'dependency-cve'`/NO `class`/**`high`** (CVSS 7.5 advisory band) with `starlette@0.38.6 (PyPI)` in the title and **no `:line`** (dep-CVEs locate to the lockfile/package), the **CVSS→enum thresholds** (each band boundary 9.0/7.0/4.0/0.1 + a real `0`→`info` + blank/absent→`null`) both directly via `CVSS_SCORE_TO_FINDING` and end-to-end through `parse`, the **severity priority** (numeric `max_severity` WINS over the `database_specific.severity` label · no group → label, `MODERATE`→medium · neither/blank-scored → `medium`, an unscored CVE is real), no-vector-leak (the raw `CVSS:3.1/…` vector NEVER dumped), the CONSTANT `classify()`→`null` + NO `securityRelevant` + no `class`, the `CVSS_SCORE_TO_FINDING`/`OSV_LABEL_TO_FINDING` map shapes, fail-safe over the degenerate `results[]/packages[]/groups/vulnerabilities` shapes, idempotent merge, schema conformance, the CLI dry-run + merge, and the **load-bearing GATE-LABEL regression** (the additive `gateLabel` `buildFinding` tweak — an OSV finding says `gated by scan-external-sca`, a semgrep finding STILL says `gated by scan-external-sast`, so the SAST output is preserved byte-for-byte); `AD1` now asserts the **9-adapter** registry — `osv` joins as the eighth file-parser]; **0.8.38 (Phase 2 · 2a #8)** adds the **`npm-audit` adapter** [the `NPM*` checks over genuine `npm audit --json` v2 output in `acceptance/fixtures/npm-audit-solano.json` (4 vulnerable packages — `body-parser`/`express`/`path-to-regexp`/`qs`; leak-clean by construction — package names + version ranges + GHSA urls only) — the EASY **Extension-A reuse**: a DIRECT severity LABEL per package via `NPM_SEVERITY_TO_FINDING` (npm's lowercase `moderate`→medium spelling; unknown/blank→`medium`), no CVSS math, REUSING OSV's path EXACTLY (the `bandFromTool` branch, the `gateLabel` param, `dimension:'dependency-cve'`, `classify()`→null) with **NO `buildFinding`/`CLASS_DEFS` change** — only the `ADAPTERS` registry line: determinism, count + band-mix (exactly 4 findings, distinct ids, 2 high · 2 medium, all `npm-audit`/`dependency-cve`/no-`class`/`package-lock.json` locus, no `:line`), the `express` anchor (→ `provenance:'deterministic'`/`engine:'npm-audit'`/`dimension:'dependency-cve'`/NO `class`/**`high`** with the package name + range in the title), the **label→band** map (each npm label + unknown/blank → `medium`) directly and through `parse`, the **via-shape matrix** (a STRING `via` → "vulnerable via …"; an OBJECT `via` → the advisory title in the message + its url as the `ruleId` and in the hit `resources`; the **PACKAGE severity wins** over the first advisory's — `qs` is package `moderate` though its first advisory is `low` → bands medium; and the raw `CVSS:3.1/…` vector a direct advisory carries is NEVER dumped), the CONSTANT `classify()`→`null` + NO `securityRelevant` + no `class` + the `scan-dependency-vulnerabilities` gate on every hit, fail-safe over the degenerate shapes (a non-object/array `vulnerabilities`, a null/non-object entry, a missing severity → a `medium` hit never dropped), idempotent merge, schema conformance, the CLI dry-run + merge, and the **NPM-gate-label** check (an npm-audit finding says `gated by scan-dependency-vulnerabilities` while OSV STILL says `scan-external-sca` and semgrep STILL says `scan-external-sast` — the per-adapter `gateLabel` is distinct, both `major`); `AD1` now asserts the **10-adapter** registry — `npm-audit` joins as the ninth file-parser]; **0.8.39 (Phase 2 · 2a #9)** adds the **`trivy` adapter** [the `TRV*` checks over genuine Trivy 0.71.2 `Class:'config'` (IaC-misconfig) output in `acceptance/fixtures/trivy-dockerfile-solano.json` (1 `FAIL` misconfig `DS-0026` "No HEALTHCHECK", Severity `LOW`, no `CauseMetadata.StartLine`; leak-clean) — done **CONFIG mode only** (the only mode with a captured fixture), REUSING checkov's **`iac-misconfig`** class (NO new `CLASS_DEFS` / `buildFinding` change — like detect-secrets reused `hardcoded-secrets`; the only shared-file touch is the `ADAPTERS` registry line): determinism, the `DS-0026` anchor → `provenance:'deterministic'`/`engine:'trivy'`/`class:'iac-misconfig'`/`dimension:'infrastructure-iac'`/class-severity **`high`**/`file:'Dockerfile'` (**no `:line`** — the real misconfig carries no `CauseMetadata.StartLine`) with the `PrimaryURL` and the "[Trivy severity LOW, recorded for reference]" note in the reasoning, the **severity-from-class consistency invariant** (mutating the misconfig `Severity` `LOW→CRITICAL` leaves the band **`high`** — class-severity, EXACTLY like Checkov; the tool tier never moves it), **class-dispatch** (a synthetic with an `os-pkgs` Vulnerabilities Result + a `config` Misconfigurations Result → ONLY the config misconfig becomes a finding; the vuln/secret classes are **Phase-2b**; `AVDID` preferred over `ID`; `CauseMetadata.StartLine` → `:line`), `Status:'PASS'`-skipped (only `FAIL` is a finding, case-insensitive), **reuses-class** (the constant `classify()`→`iac-misconfig` is the SAME `CLASS_DEFS` entry checkov uses — `Object.keys(CLASS_DEFS)` unchanged at the original 5, NO `trivy` entry), classify/fail-safe (`securityRelevant===undefined`; the degenerate `Results` / Misconfiguration / `secret`-class shapes → `[]`/skipped, no crash), idempotent merge, schema conformance, and the CLI dry-run + merge; with two IaC engines now live, Checkov↔Trivy on the SAME Dockerfile misconfig (`CKV_DOCKER_2`↔`DS-0026`) is the visible **SAFE under-merge** — collapsing it is §10 ext #3, Phase-2b; `AD1` now asserts the **11-adapter** registry — `trivy` joins as the tenth file-parser]; **0.8.40 (Phase 2 journey-wiring)** adds the **content-shape recognizer + `--all` mode** [the `RC*` + `ALL*` checks: every committed fixture → its OWN adapter by CONTENT SHAPE not filename, a clean `results:[]` scan still recognized, non-adapter shapes (the evidence `index.json` / a `retire` shape / an `openapi` spec / the toolkit's `deps-npm` disposition WRAPPER) → `null`, a synthetic 2-match → `{ambiguous:[…]}` never a guessed name, failsafe (`null`/`{}`/`{results:null}`/non-object → `null`, no throw; `[]` → gitleaks per the proven predicate); and `--all` recognizes + ingests every RENAMED scanner output under `evidence/` (`checkov`→`iac-*`, `gitleaks`→`secret-scan-*`, `npm-audit`→`deps-npm-*`), skips the non-adapter `index.json` (named), is BYTE-DETERMINISTIC run-to-run, reports Code-Analyzer-absent → `PENDING-OWNER-RUN` (never LLM-fill) while present → `crud-fls` band appears, and PRESERVES the secret-never-leaks invariant (no `Match`/`Secret`/`hashed_secret` token reaches the ledger); `AD1` stays at **11 adapters** — no new adapter, the recognizer routes the existing ten file-parsers]), the **LLM-supersession enforcement** (`test-reconcile-provenance` — the 0.8.29 Phase-1·Slice-2 guard over `harness/reconcile-provenance.mjs`: a `deterministic` finding in the SAME owned class at the SAME locus [reusing `finding-clusters.mjs` `sameLocation`] demotes a co-located `llm-inferred` finding → `status:'superseded'` + `superseded_by`(det id) — so the LLM can never re-report/re-judge what an engine determined; CONSERVATIVE [a different class / a different locus / an UNMAPPED deterministic finding that owns no class → UNTOUCHED; precise class match with a dimension fallback; mark-not-delete] + PURE + IDEMPOTENT [reconcile twice → byte-identical, 2nd pass supersedes 0] + owner/terminal LLM states preserved + the CLI reconciles a target ledger / `--dry-run` doesn't write / a corrupted ledger is refused + a superseded finding validates against the extended `$defs/finding`), the **deterministic-findings INTEGRATION wiring** (`test-deterministic-integration` — the 0.8.30 Phase-1·Slice-3 guard (extended 0.8.40) that drives the REAL CLI SEQUENCE end-to-end on a tmp ledger off the committed `acceptance/fixtures/`: the deterministic pass [ONE `--all` invocation — metadata-viewall ALWAYS + the content-recognized `code-analyzer-*.json`, the 0.8.40 single-pass band] seeds `provenance:'deterministic'` CRUD/FLS + ViewAll findings, a co-located same-class LLM finding is SUPERSEDED by `reconcile-provenance` while off-class / off-locus LLM findings SURVIVE, reconcile is idempotent + the reconciled OPEN band drops the superseded finding; plus the **WIRING** — audit-codebase GRANTS + INVOKES both harnesses with the `--all` ingest BEFORE the LLM fan-out [`build-audit-engine.mjs --plugin`] and the reconcile AFTER the merge [`merge-ledger.mjs`] + the `sf`-absent → PENDING-OWNER-RUN, never-LLM-fill note, the journey REFERENCES both with the same ordering + note, and **0.8.40 W10/W11** — run-scans GRANTS both harnesses and INVOKES `--all` then `reconcile-provenance --target` at its scan tail (in order) + the PENDING-when-absent note), the **cross-run recurrence classifier** (`test-recurrence-confidence` — the 0.8.7 engine that classifies findings across N independent run-ledgers of the same codebase: the all-runs-stable→`confidence=high` reliably-recurring-blocker case, the confirmed→refuted→absent flip→`some_runs`/`review`, the severity-drift (medium/high/medium)→`all_runs`/`review`-not-`high`, the single-run→`investigate`, line-span overlap MATCH vs non-overlap STAY-SEPARATE + absolute-vs-relative path-suffix reconciliation, the **0.8.8 bare-basename over-merge guard** (a single-segment file like `package.json` cited in three different directories stays THREE single_run loci, never one all_runs/high — false confidence is the forbidden direction — while an identical root-level `Dockerfile` cite still merges by exact equality), the **0.8.8 two-phase anti-bridge guard** (a broad refuted finding overlapping two disjoint confirmed defects attaches to one without FUSING them), the **0.8.10 commit-consistency guard** (same `audited_commit` across runs→`consistent` + no mixed-warning; differing→`mixed` + caveat warns code-change-vs-instability; a run missing its commit→`unknown`), the **0.8.10 by_file rollup** (2 loci in fileA + 1 in fileB → 2 file rows, fileA `locus_count=2` with the right confidence tally + `has_reliable_blocker`), the **0.8.10 `--repo-root` relativization** (a path under the root is stripped to display-relative, a path not under it is left intact, matching unaffected), byte-determinism, fail-closed [non-array `findings`→empty; missing/non-JSON ledger→exit 2], the N=1 no-recurrence-signal caveat, hand-checkable pairwise Jaccard (2/3→0.67), and the honesty-caveat presence + no-completeness-assertion guard — INLINE synthetic fixtures, so it runs in CI without the off-repo real ledgers), the **baseline-refutation check** (`test-baseline-refutation-check` — the 0.8.16 v67 auto-enforcement gate: flags `refuted` findings citing platform auto-enforcement [user-mode/with-sharing at API 67.0+] the package `sourceApiVersion` <67.0 doesn't buy — 64→invalid / 68→valid / null→unknown, version precedence `--api-version`>`--sfdx-project`>`--scope-manifest`, `--strict` exits 3, byte-determinism, fail-closed; REPORT-ONLY, gates nothing), the **union-convergence check** (`test-union-convergence` — the 0.8.16 Thread-2 engine: does the union of confirmed loci across N runs STOP growing? a converging set→`converged`+`plateau_run` set, a non-converging set→`converged:false`/`plateau_run:null`, overlapping-line-span locus identity shared with the recurrence engine, OPEN-only, byte-determinism + the ≥2-run guard; REPORT-ONLY, gates nothing), the **audit-engine assembler** (`test-build-audit-engine` — §4/§5 extraction + inject passes injection-check, loud failure on a bad dimension; plus the **0.8.17 engine-enforced always-on dimensions** — `sessionid-egress`/`secrets-credentials`/`error-handling-disclosure` are auto-injected regardless of the driver's scope-input, an always-on key in `na` is forced applicable with a WARN, and nothing duplicates when the driver already lists them), the **artifact-engine assembler** (`test-build-artifact-engine` — the **0.8.21 P2-parity twin** for the ARTIFACT phase: valid `{artifacts,facts,gate}` DATA → `artifact-engine.mjs` carrying repoRoot + the pre-read templates; a missing template / empty focus / absent injection marker each abort loud; **GATE ENFORCEMENT** — a `gate.suppress` artifact is DROPPED + warned, so a withheld doc physically can't be drafted; the template's run-args guard fires against the real source; every-artifact-withheld → exit 2; byte-determinism — so neither phase hand-authors a Workflow with inline prompt strings), the **gate-spec engine** (`test-gate-spec` + `test-tier-no-reask` — the **0.8.22 presentation-consistency Slice 1**: the FROZEN gate catalog + pure `gateOptions(gateId,facts)` selector that PINS each `AskUserQuestion` gate's option set so the driver renders `label`/`description` VERBATIM + pipes the chosen `decision` to `record-consent` — determinism [same `(gate,facts)` twice → byte-identical, incl. via the CLI], golden option snapshots [run-mode's 2-option election / audit-tier's first-pass `{Standard,Exhaustive,Quick,Cancel}` with **Exhaustive OFFERED but never pre-selected** / the WI-02 confirm variant `{Authorize the <locked> launch, Change tier, Cancel}` / scanner-install's VERBATIM sha256 / tmp-removed / evidence-kept / "this yes also covers RUNNING them, which fetches rules" disclosure with only the count + `name(method)` list filled], **FAIL CLOSED** [an unknown gate / an option missing `label`·`description`·`decision` / a decision that is not `affirm|deny` / scanner-install with zero installable scanners each THROW], the **FORCE-INJECTED safe-default decline** present on EVERY consent gate [proven even when the selector's own set omits it — the `ALWAYS_ON` pattern] but NOT on the non-consent run-mode election, every emitted `decision` round-tripping through the real `recordConsent` [`affirm`→affirmative, `deny`→not], and the **WI-02 collect-once flow** [a journey→audit-codebase CLI fixture asserts the tier menu is collected ONCE and the second surface is a confirm, not a re-election; pass-1 never pre-selects Exhaustive; only "Change tier" re-opens the menu] + the journey / audit-codebase render-verbatim wiring), the **operator-output render-verbatim substrate** (`test-render-stability` + `test-readiness-verdict` — the **0.8.23 presentation-consistency Slice 2**, the OUTPUT-class twin of gate-spec: `render-stability.mjs` emits the VERBATIM Finding-Stability block from `recurrence-confidence.json` [determinism; PRESENT (n≥2) = the `bucket_counts` table + the `reliably_recurring_blockers` set + the contestable band named consistently + a mixed-commit note IFF `commit_consistency != consistent`; ABSENT/single-run = one honest one-liner; the "informational only — changes NOTHING about the SCI gate" caveat on BOTH branches, and it NEVER claims complete/passed], and `render-readiness-verdict.mjs` + `templates/operator/readiness-verdict.md.tmpl` PIN the readiness verdict [fill on a frozen fixture → byte-identical twice; the fixed RENDER-sentinel section order; the **`SCI_BLOCK` slot equals `compute-sci` stdout BYTE-FOR-BYTE**; the standing caveat equals the single committed `STANDING_CAVEAT` constant, force-injected so a caller value is OVERRIDDEN (un-paraphrasable); **FAIL CLOSED** — a missing slot leaves a `{{SLOT}}` → `fillVerdict` THROWS, a template without the caveat slot THROWS, no `{{…}}` survives a real fill; the `lintRenderVerbatim` flags a hand-built Markdown table for a surface with a registered renderer/template, and the real compile-submission skill passes by routing through the template]), the **entry-experience render-verbatim set** (`test-finding-clusters-headline` + `test-render-target-map` + `test-render-preflight` + `test-render-scan-status` + `test-render-router-status` + `test-render-recap` — the **0.8.24 presentation-consistency Slice 3**, pinning the six most-seen ENTRY surfaces: the **finding-cluster triage headline** [`renderClusterHeadline` + `--headline` — raw per-severity counts FIRST then the clustered distinct-file view; the SAME block at the audit exec summary AND the journey blocker gate, so the FAILURE VERDICT reads byte-identically; NONE → an honest "nothing new" line, a missing ledger → "unavailable", never a false clean], the **target-map approval** [fixed `{dimension|applicable|targets|why|confidence|unresolved}` table, applicable rows FIRST, unresolved flagged, pipe/newline-escaped cells], the **3-tier preflight** [`DEEP_AUDIT_STATES` is EXACTLY the four states + `deepAuditState` is TOTAL over package-readiness's outputs (installable / needs-build+registered / needs-build+!registered / no-package); the three tiers render in fixed order; empty/null facts → all-"not detected", never fabricated], the **scan-status summary** [the frozen 8-row `SCAN_FAMILIES` in canonical Family 1–8 order with locked columns; DONE needs a reviewer-reproducible report on disk, a pending-owner plan → PENDING, a structural-N/A → N/A; the manifest drives Applies, no manifest → "?"], the **router "where are we?"** [exactly 3 lines; the phase ladder maps each furthest-reached phase to the right next skill; drift → re-scope, a stale ledger → re-audit; null → fresh start], and the **end-of-run recap** [LED BY `renderClusterHeadline` — a byte-identical-lead assertion proves the recap embeds the headline verbatim; the counts line; PROCEED/HALT from the open critical/high; the fixed not-covered caveat; merge-ledger imports + emits it to stdout]; each test asserts determinism + a golden snapshot of the fixed structure + fail-safe on missing/non-JSON input + the skill-wiring [grants + references the harness + states "print verbatim"]), the **scope-submission REPORT render-verbatim set** (`test-render-detected-elements` + `test-render-mcp-scope` + `test-render-sf-autoresolve` + the extended `test-applicable-requirements` — the **0.8.25 presentation-consistency Slice 4**, WI-06 reports half, pinning the five scope-submission REPORT surfaces: the **detected-architecture-elements summary** [INV-15 — the fixed `{Element|Detected how (evidence)}` table in `CANONICAL_ELEMENT_ORDER`, unknown types appended/never dropped, the listingType line, a no-evidence cell honest, empty → "scope not detected yet"], the **applicable-requirements presentation** [INV-16 — `applicable-requirements.mjs --render`: count = exact list length, ids by track, the conflicting-requirements section surfacing the `endpoint-ssl-labs-a-grade` `conflicting` entry WITH its folded-block-scalar `conflicts` text per CONVENTIONS §4, the mobile-no-coverage gap line when a mobile element is in scope, empty → "scope not computed yet"; plus the `parseBaselineApplies` verification/conflicts parse], the **MCP direction + auth profile** [INV-43 — B/A/both captions + the auth-profile fields rendered straight from `mcp.authExpectations`, NOT re-derived; missing authExpectations → honest, never fabricated], the **MCP live-probe result** [INV-44 — `probed:true` → "live-probed", `probed:false` → "recorded from code, NOT live-probed" (an un-probed fact is NEVER presented as a probe), an absent field → "(not recorded)"], and the **SF-CLI auto-resolution** [INV-45 — the rows table + a Security-flags section DERIVING every class (http:// non-TLS · wildcard · no-Named-Credential · ViewAll/ModifyAll over-grant), merged/deduped, never dropped + a Conflicts section (CLI is evidence, not an override); gated on the manifest `sfAutoResolved` → "auto-resolution skipped" when it didn't run; and a **secret guard** — a secret-named key / token-shaped value is REDACTED, never echoed (CONVENTIONS §6); **0.8.26** extends the secret guard to EVERY rendered cell (derived-flag host, row/endpoint/flag/conflict source, recorded-flag detail, conflict autoResolved), not just row values, and softens the docstring from an overclaimed "never" to its honest producer-is-the-boundary scope (SA6)]; plus the **dict-vs-array honesty guard** folded into `test-finding-clusters-headline` + `test-render-recap` [a PRESENT-but-non-array `findings` renders UNAVAILABLE, never NONE/PROCEED — the CLAUDE-rule-8 corollary; **0.8.26** extends it to the `render-recap.mjs --target` path — `factsFromLedger` preserves the dict shape instead of coercing it to `[]`, so the standalone re-render is UNAVAILABLE not a false PROCEED (RC7)], the **scope-submission GATES render-verbatim set** (`test-scope-gates` + `test-render-scope-summary` — the **0.8.27 presentation-consistency Slice 5**, WI-06 gates half, COMPLETING WI-06: the gate-spec catalog gains six scope-submission gates split into TWO semantic classes by a new load-validated `kind` field (`consent ⟺ kind:'consent'`): the **CONSENT-to-act** gates — `mcp-probe` (INV-30, the live read-only-probe gate whose `STAGING`/`PRODUCTION` choice IS the environment confirmation so production is never probed silently, `{{URL}}` the only fillable datum via a function-replacer, throws without a url) and `scope-confirm` (INV-06, the final-manifest confirm mirroring the WI-02 audit-tier variant: `{Confirm & proceed, Correct the scope, Cancel}`, both non-confirm options the FAIL-SAFE deny) — which force-inject the decline + pipe the chosen `decision` through the real `recordConsent`; and the **ANSWER** gates — `partner-program` (INV-05, the SIX preflight sub-gates via `facts.subGate`, each a fixed `Yes→affirm/No→deny` recorded into `operatorConfirmed.<key>`, the `promoted` gate offering `N/A — no package in scope` ONLY when `facts.noPackage`), `clarify-detection` (INV-31, the NEED-FROM-YOU `present/absent/unsure` gate, `{{ELEMENT}}` filled, throws without an element), and `listing-type`/`tenancy` (INV-32, the categorical closed choices, all-`affirm`, the chosen LABEL recorded) — which carry NO force-injected decline and are NOT piped (the selection is recorded into the manifest). `test-scope-gates` asserts per-gate determinism + golden option snapshots + the fail-closed throws + the force-injected decline on the CONSENT gates / none on the ANSWER gates + the `kind` semantics consistency across the whole catalog + the consent round-trip through `recordConsent` + the promoted-N/A conditional + the `$`-bearing-URL-literal guard + the scope-submission wiring; `test-render-scope-summary` pins the **final scope-manifest summary** (`render-scope-summary.mjs` INV-06 — the fixed Step-9 readout in fixed field order: listingType · direction · auto-resolution · repo commit · element list in `CANONICAL_ELEMENT_ORDER` · endpoints WITH environment labels (a missing one flagged `⚠ UNLABELED`) · the applicable count = exact list length · the `operatorConfirmed` gate states rendered HONESTLY (✓/✗/not-recorded, NEVER a fabricated ✓); a missing/non-JSON/non-object manifest → "scope not finalized", NEVER a fabricated "ready/confirmed") with determinism + golden skeleton + fail-safe + `REGISTERED_SURFACES` registration + skill wiring), the **dimension-extraction coverage** (`test-dimension-extraction` — drives build-audit-engine over EVERY `methodology/dimensions/*.md` so every current + future dimension's §4/§5 markers + non-empty finder/verifier prompts are guarded, not just two hand-picked keys), the artifact gate on every entry path (incl. the audit-only auto-proceed + the authN/authZ withhold), the **PreToolUse hook** (no-op unless armed + writing the gated artifact → denies; fail-closed), the **sf-ops safety gate** (`test-sf-ops-gate-hook` — the 0.8.11 fail-closed consent gate for irreversible sf/host ops: each of the 3 gate classes [`sf-package-promote` / `sf-deep-audit-ops` / `sf-cli-setup`] classified verb-based and DENIED without a recorded consent / ALLOWED with one; the PERMANENT `sf package version promote` is its OWN gate [a deep-audit consent does not authorize it, and vice-versa] with a permanence-emphasizing deny; read-only + benign commands pass [`sf org list`, `sf package version list`, `--help`, `git push`, `ls`, local `npm install`]; variant normalization [colon form, `sfdx force:`, leading `FOO=bar sudo`, whitespace, a CHAINED command DENIED on its promote segment]; scope + fail-closed [no `.security-review/` up-tree → ALLOW, malformed/empty payload → ALLOW so arbitrary Bash is never blocked, a classified op with no consent → DENY] + the real CLI deny-JSON/allow-no-stdout path; plus the **0.8.12 adversarial bypass battery** — ~36 wrapped/normalized forms of the same irreversible op that must all DENY [CLI paths `/usr/local/bin/sf`/`./sf`, quoted/escaped CLI `"sf"`/`\sf`, background `&`/`|&`, grouping `(…)`/`{…;}`/`((…))`, wrappers `command`/`exec`/`time`/`nice`/`nohup`/`watch`/`sudo -u nobody`/`env -i`, `sh -c`/`eval` (incl. a separator INSIDE the quoted inner command + a chained `… && bash -c "…"`), interspersed verb-scan flags `--json`/`-o foo`/`--org=foo`/`--`/leading-flag-before-CLI, the newly-gated `package delete`/`package version delete`/`sandbox create`/`sandbox delete`/`npm uninstall -g`], a benign no-false-denies set, and the EXOTIC residual [`$(…)`/backticks/`$CMD`/`source <(…)`/base64-eval] that stays ALLOW by design), the **deployed-audit readiness** check (placeholder/`needs-build` vs `installable`), element-precise applicability, baseline-count consistency (incl. the **0.8.20 `--currency` emission** — `test-baseline-counts` asserts `newest_verified`/`newest_verified_count`/`oldest_verified` with `null`/malformed date tokens EXCLUDED from the ISO-lexicographic ranking, so a malformed token can never out-rank a real date), **baseline-entry integrity** (`test-baseline-integrity` — every entry's `applies_to` tokens / `severity_if_missing` / the `verification ⟺ last_verified` per-entry implication, plus the 2026-06-20 PMD-rule prediction presence), **prose hygiene** (`test-prose-hygiene` — the CONVENTIONS §9 "simply" ban + the §3 partner-internal-symbol ban, encoded after an adversarial audit caught a voice/genericization slip so it can't recur), cross-dimension de-dup, the audit-engine **injection pre-launch check** (decoy-anchored), the **CI-workflow hygiene lock** (`test-ci-hygiene` — asserts `.github/workflows/test.yml` declares a top-level least-privilege `permissions: contents: read` block and NO write scope, so the public-repo workflow's `GITHUB_TOKEN` can never silently widen; flagged by an external security audit, 0.8.19), the **failure-log clamp** (`test-clamp-log` — the 0.8.20 head+tail truncation `clampLog` keeps the ROOT CAUSE at the top of a deep stack trace, not just the tail; a short string is unchanged, a long one is bounded with an elision marker; used by `run-dast` + `install-scanners` in place of tail-only `.slice(-1500/-2000)`), and ledger staleness — across three layers: the pure `staleFindings` unit test, a **hermetic detect-path test** (`-detect`), and an **adversarial test** (`-adversary`). No LLM, no scanners, no off-repo fixtures — the only committed test data is the small real captured scanner output under `acceptance/fixtures/` (Code Analyzer JSON + the genuine Checkov 3.3.2 dockerfile output + the genuine Semgrep OSS SAST output + the genuine Bandit Python-SAST output + the genuine njsscan 0.4.3 Node-SAST output + the genuine gitleaks secret-scan output + the genuine detect-secrets 1.5.0 secret-scan output + the genuine OSV-Scanner SCA output + the genuine `npm audit --json` v2 output + the genuine Trivy 0.71.2 IaC-misconfig output, leak-clean by construction — gitleaks' synthetic redacted secrets, detect-secrets' hashes-only-by-design, OSV's lockfile path genericized, npm-audit's package-names-and-ranges-only, trivy's generic-Dockerfile-guidance-only); plus the **0.8.43 detection-accuracy set** (`test-gate-message-clarity` — the deep-audit ↔ scanner-install `sf` cross-reference [authed-global vs unauthed-tmp] + the audit-launch confirm-variant authorize/launch framing; `test-package-readiness`'s NESTED-SFDX `discoverPackages` discovery [nested-only repo → finds both, classifies each, anyInstallable roll-up; single-root legacy shape preserved]; `test-render-preflight` PF7's installable+sf-absent "pending sf install" qualifier; `test-install-scanners` CA9's deterministic Code-Analyzer plugin-version readout); `node` runs them all. |
| `acceptance-report-<date>.md` | The graded result of a fixture run: per-class recall, precision on the negative controls, and every gap the run surfaced (each encoded into the toolkit and re-proven). |
| `integration-pass-condition-<ver>.md` | The PRE-COMMITTED, write-before-run pass condition for a version's full-journey validation — authored before the run, graded **cold in a fresh restarted session** off disk. A clean pass gates that version's release **tag**. (`-0.6.0` [consented-scanner-install] + `-0.7.0` [throwaway-DAST + the credential contract] **both PASSED** in the 2026-06-19 cold run — one full Atlas journey graded off disk → **`v0.7.0` tagged**; `-0.5.1`/`-0.5.2` passed earlier, checkpoints tagged through v0.5.5.) |

## Run it

```bash
node acceptance/generate-fixture.mjs ~/srt-helios
# scope the fixture (a fresh agent following scope-submission, or the skill itself)
#   → writes ~/srt-helios/.security-review/{scope-manifest,target-map}.json
node acceptance/build-run-args.mjs "$PWD" ~/srt-helios 2026-06-15
#   → Workflow({scriptPath: "~/srt-helios/.security-review/audit-engine.mjs"})
gitleaks git ~/srt-helios --redact     # run-scans Family 6, history pass
```

Then grade the confirmed findings against `expected-findings.md`. A planted class
the run misses means the encoding was not forceful enough — sharpen the dimension
file (not the session) and re-run. A negative control that gets flagged means the
verifier over-fires — tighten the §5/§6 refute rules.

## Standing tests (deterministic — no fixture, no LLM, no scanners)

The `harness/` engines (and the `hooks/` enforcement hook) that encode the
honesty-critical determinizable properties each carry a self-asserting test. Run
the whole suite (exit 0 = pass; the file/check count grows each checkpoint — the
pass is ZERO failures, not a fixed number; see the table at the top of this file
for the current totals):

```bash
for t in acceptance/test-*.mjs; do node "$t" || exit 1; done
```

They assert, respectively: the SCI fails CLOSED on an empty/missing manifest and
is byte-identical on re-run (currency floor included); the **artifact gate** is
audit-only (clean / flagged — there is NO STOP mode) and withholds the AuthN/AuthZ
artifact only for an open critical/high authN/authZ finding, **failing SAFE
(withhold) on a malformed non-array ledger** rather than reading it as clean; the
**G4 PreToolUse hook** is a no-op unless armed + writing the gated artifact, denies
over an open hole, and **fails CLOSED** on an unreadable/malformed ledger; the
**audit-engine injection check** anchors past the header-comment decoy; the
**deployed-audit readiness** check reads `installable` / `needs-build` /
`no-package` and is bound to the configured package (an unrelated/dependency `04t`
alias cannot fake `installable`); applicability drops agentforce-* / mcp-* for a
plain package and keeps them for a real agent/MCP one; the baseline self-description
counts match the prose; cross-dimension findings de-dup to a distinct-file
headline; and ledger staleness flags findings whose code changed since their
`audited_commit` — proven at three layers: the pure `staleFindings` unit test, a
hermetic detect-path test that drives the git-shelling CLI against a throwaway
repo, and an adversarial test over the messy real-world `finding.file` shapes
(comma/range suffixes, two-file cites, absolute paths). A refactor that breaks any
of these fails the test — that is what makes the enforcement real rather than
narrated.

The fixture is intentionally *not* committed (it carries planted vulnerabilities
and a git-history secret); it is regenerated on demand from the deterministic
generator, which keeps the proof reproducible without shipping a vulnerable
package or a live-shaped secret in this repo.
