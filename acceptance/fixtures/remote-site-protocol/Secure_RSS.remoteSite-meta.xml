<?xml version="1.0" encoding="UTF-8"?>
<!--
  Authored source-scanner fixture for harness/ingest-scanner-findings.mjs
  (remote-site-protocol-security adapter). Synthetic toolkit test data — NOT a
  partner artifact.
  NEGATIVE (the false guard): <disableProtocolSecurity> is explicitly false —
  the platform default, protocol security intact -> NOT flagged. Only a true
  value permits the HTTPS<->HTTP transfer the Secure Communication requirement
  forbids; an explicit false element must stay clean.
-->
<RemoteSiteSetting xmlns="http://soap.sforce.com/2006/04/metadata">
    <disableProtocolSecurity>false</disableProtocolSecurity>
    <isActive>true</isActive>
    <url>https://partner.example.com</url>
</RemoteSiteSetting>
