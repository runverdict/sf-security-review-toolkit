<?xml version="1.0" encoding="UTF-8"?>
<!--
  Authored source-scanner fixture for harness/ingest-scanner-findings.mjs
  (egress-plain-http adapter). Synthetic toolkit test data — NOT a partner artifact.
  POSITIVE: the MODERN (API 56.0+) NamedCredential shape — the endpoint lives in the
  <namedCredentialParameters> block's <parameterValue> (sibling <parameterType> Url),
  declared over plain http:// -> FLAGGED. Proves the modern-shape extraction path;
  its https:// sibling fixture (Modern_NC) proves the same path does not over-flag.
-->
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>Modern Plain HTTP Integration</label>
    <namedCredentialType>SecuredEndpoint</namedCredentialType>
    <namedCredentialParameters>
        <parameterName>Url</parameterName>
        <parameterType>Url</parameterType>
        <parameterValue>http://callout.example.com</parameterValue>
    </namedCredentialParameters>
</NamedCredential>
