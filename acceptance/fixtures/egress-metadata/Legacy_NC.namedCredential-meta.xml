<?xml version="1.0" encoding="UTF-8"?>
<!--
  Authored source-scanner fixture for harness/ingest-scanner-findings.mjs
  (egress-plain-http adapter). Synthetic toolkit test data — NOT a partner artifact.
  POSITIVE: the LEGACY NamedCredential shape holds the endpoint in <endpoint>,
  declared over plain http:// -> FLAGGED. No secret value exists in this file
  (credential secrets are org-encrypted, never in metadata) — the finding is the
  insecure-transport endpoint only.
-->
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <endpoint>http://legacy.example.com</endpoint>
    <label>Legacy Integration</label>
    <principalType>Anonymous</principalType>
    <protocol>NoAuthentication</protocol>
</NamedCredential>
