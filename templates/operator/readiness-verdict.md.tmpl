# {{SOLUTION_NAME}} — Security Review Readiness Verdict

> Generated by `/sf-security-review-toolkit:compile-submission` on {{RUN_DATE}}. Every
> section below is rendered VERBATIM from a deterministic engine — the blocks are pasted
> byte-for-byte and never hand-edited (CONVENTIONS §7). The `<!-- RENDER:… -->` sentinels
> bound each engine block; re-run the engines to refresh the verdict, never edit in place.

<!-- RENDER:sci-block START — paste `compute-sci.mjs` stdout byte-for-byte; the headline gate; never hand-edit the number -->
{{SCI_BLOCK}}
<!-- RENDER:sci-block END -->

## Ledger Freshness

<!-- RENDER:ledger-freshness START — `ledger-staleness.mjs` non-json one-liner; if `stale`/`no-fingerprint`, degrade the readiness language and recommend a re-audit -->
{{LEDGER_FRESHNESS}}
<!-- RENDER:ledger-freshness END -->

<!-- RENDER:finding-stability START — `render-stability.mjs` block; INFORMATIONAL ONLY — it changes nothing about the SCI gate or the band -->
{{FINDING_STABILITY}}
<!-- RENDER:finding-stability END -->

## Per-category readiness

<!-- RENDER:per-category START — ready / not-ready per the tracker's section boundaries (docs · package code-scan · external-endpoint · CI scanning · test environment · listing lifecycle), each blocking row named -->
{{PER_CATEGORY}}
<!-- RENDER:per-category END -->

## Blockers

<!-- RENDER:blockers START — one line each: the row, the owner, the concrete closing action (never "complete security testing") -->
{{BLOCKERS}}
<!-- RENDER:blockers END -->

## What was NOT verified, and how the rest was

<!-- RENDER:not-verified START — the white-box audit is static review (not DAST/pen test); owner-run scans verified only as on-disk files; "wired into CI" is workflow+output, not a clean baseline; anything resting on a credential this skill couldn't use is owner-asserted; state the tier + pass count -->
{{NOT_VERIFIED}}
<!-- RENDER:not-verified END -->

## Open conflicting baseline entries

<!-- RENDER:open-conflicting START — each with: confirm via your Partner Account Manager / partner Slack before relying on it; never resolved silently -->
{{OPEN_CONFLICTING_BASELINE}}
<!-- RENDER:open-conflicting END -->

## Standing caveat

<!-- RENDER:standing-caveat START — the CANONICAL constant from render-readiness-verdict.mjs; force-injected, never paraphrased -->
{{STANDING_CAVEAT}}
<!-- RENDER:standing-caveat END -->
